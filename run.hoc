{xopen("templates/defvar.hoc")}

default_var("coredat", "coredat")

// create directory for writing dataset
{
    strdef cmd
    nrnpython("from commonutils import mkdir_p")
    sprint(cmd, "mkdir_p('%s')", coredat)
    nrnpython(cmd)
}

strdef parameters
parameters="./parameters/Control.hoc"

xopen("main.hoc")


